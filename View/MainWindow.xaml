<Window x:Class="BookMedicinalPlants.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookMedicinalPlants"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <ListBox Grid.Row="0" ItemsSource="{Binding Plants}" Background="Green" Foreground="White" SelectedItem="{Binding MySelectedPlant}">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <Border>
                    <StackPanel >
                        <!--<local:UserControlPlant MynamePlant="{Binding Path=Name}" MypublicnamePlant="{Binding Path=PublicName}" MyphotoPlant="https://yellow.ua/media/catalog/product/cache/9/image/508x508/9df78eab33525d08d6e5fb8d27136e95/2/1/2184907.1800x1800w.jpg"/>-->
                        
                        
                    </StackPanel>
                </Border>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</Window>
